# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# GitHub API Configuration (optional, for higher rate limits)
# Get your token from: https://github.com/settings/tokens
GITHUB_ACCESS_TOKEN=your-github-token-here

# Database Configuration
DATABASE_URL=sqlite:///learning_copilot.db

# Application Settings
DEBUG_MODE=False
MAX_TOKENS=4000
# Note: GPT-5 models don't support custom temperature (always 1.0)
# TEMPERATURE only applies to GPT-4 and earlier models
TEMPERATURE=0.7

# Model Selection
# Options: gpt-5, gpt-5-mini, gpt-5-nano, gpt-4, gpt-4-turbo-preview
# Recommended: gpt-5-mini (cost-effective with good performance)
# Note: GPT-5 models have specific API restrictions:
#   - No temperature control (always 1.0)
#   - Use max_completion_tokens instead of max_tokens
#   - Support verbosity and reasoning_effort parameters
MODEL_NAME=gpt-5-mini